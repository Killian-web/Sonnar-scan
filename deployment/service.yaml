apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: dev
  labels:
    app: account-service
spec:
  type: NodePort
  selector:
    app: account-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
